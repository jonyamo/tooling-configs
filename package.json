{
  "name": "jonyamo-style-configs",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "preinstall": "/usr/bin/env bash -c \"[[ $npm_execpath = *'yarn'* ]] || (echo 'use yarn' && exit 1)\""
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
